// Stub — real settings (brand profile + account + billing) lands in H-0e.
export default function SettingsStubPage() {
  return (
    <main className="min-h-screen bg-neutral-950 text-white">
      <div className="max-w-2xl mx-auto px-6 py-12 lg:py-20">
        <p className="text-xs uppercase tracking-[0.2em] text-[#4ef2d9]">
          Simply Edit Pro Studio
        </p>
        <h1 className="text-3xl font-bold mt-1">Settings</h1>
        <p className="text-sm text-neutral-400 mt-3 max-w-lg">
          Settings UI lands in H-0e — brand profile, account details, billing
          management.
        </p>
      </div>
    </main>
  );
}
